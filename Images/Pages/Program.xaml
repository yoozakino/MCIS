<Window x:Class="Информационная_система_медицинской_клиники.Program"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Информационная_система_медицинской_клиники"
        mc:Ignorable="d"
        Title="ИСМК" Height="700" Width="800" ResizeMode="NoResize"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        
    <LinearGradientBrush x:Key="HoverGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#FFA500" Offset="0"/>
        <GradientStop Color="#FFD700" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="ClickGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#FFA500" Offset="0"/>
        <GradientStop Color="DeepSkyBlue" Offset="1"/>
    </LinearGradientBrush>

        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FFBEE6FD"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" CornerRadius="7" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource HoverGradientBrush}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource ClickGradientBrush}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Window.Background>
        <ImageBrush ImageSource="/Images/main_background.jpg" Stretch="UniformToFill"/>
    </Window.Background>
    <Grid>
        <StackPanel VerticalAlignment="Top" Height="50" Width="650" Margin="20">
            <Button x:Name="create_record" Width="200" Height="30" Content="Добавить запись" HorizontalAlignment="Left" VerticalAlignment="Top" FontFamily="PlumbBoldC" FontSize="17" Background="Gold"/>
            <Button x:Name="delete_record" Width="200" Height="30" Content="Удалить запись" HorizontalAlignment="Right" VerticalAlignment="Top" FontFamily="PlumbBoldC" FontSize="17" Margin="0, -30" Background="Gold"/>
            <Button x:Name="Update_table" Width="200" Height="30" Content="Обновить данные" HorizontalAlignment="Center" VerticalAlignment="Top" FontFamily="PlumbBoldC" FontSize="17" Margin="0, -30" Background="Gold"/>
        </StackPanel>

        <StackPanel Margin="10,75,600,7">
            <Button Content="Пациенты" Height="40" FontFamily="PlumbBoldC" FontSize="17" Background="Gold"/>
            <Button Content="Врачи" Height="40" FontFamily="PlumbBoldC" FontSize="17" Background="Gold" Margin="0, 20"/>
            <Button Content="Записи на прием" Height="40" FontFamily="PlumbBoldC" FontSize="17" Background="Gold"/>
            <Button Content="Медицинские карты" Height="40" FontFamily="PlumbBoldC" FontSize="17" Background="Gold" Margin="0, 20"/>
            <Button Content="Услуги" Height="40" FontFamily="PlumbBoldC" FontSize="17" Background="Gold"/>
            <Button Content="Расписание врачей" Height="40" FontFamily="PlumbBoldC" FontSize="17" Background="Gold" Margin="0, 20"/>
            <Button Content="Кабинеты" Height="40" FontFamily="PlumbBoldC" FontSize="17" Background="Gold"/>
            <Button Content="Выставленные счета" Height="40" FontFamily="PlumbBoldC" FontSize="17" Background="Gold" Margin="0, 20"/>
            <Button Content="Лекарства" Height="40" FontFamily="PlumbBoldC" FontSize="17" Background="Gold"/>
            <Button Content="Назначения лекарств" Height="40" FontFamily="PlumbBoldC" FontSize="17" Background="Gold" Margin="0, 20"/>
        </StackPanel>
        <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
